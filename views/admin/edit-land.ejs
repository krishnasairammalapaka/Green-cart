<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Land Record - Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="min-h-screen flex">
        <%- include('../partials/admin-sidebar', { currentPage: 'farmer-info' }) %>

        <div class="flex-1 p-8">
            <div class="max-w-2xl mx-auto">
                <h2 class="text-3xl font-bold text-gray-800 mb-8">Edit Land Record</h2>

                <form action="/admin/update-land/<%= land.id %>" method="POST" class="bg-white rounded-lg shadow-md p-6">
                    <div class="space-y-6">
                        <div>
                            <label class="block text-sm font-medium text-gray-700">Land Number</label>
                            <input type="text" 
                                   name="land_number" 
                                   value="<%= land.land_number %>"
                                   class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700">Farmer Name</label>
                            <input type="text" 
                                   name="farmer_name" 
                                   value="<%= land.farmer_name %>"
                                   class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700">Area of Land (acres)</label>
                            <input type="number" 
                                   name="acres_of_land" 
                                   value="<%= land.acres_of_land %>"
                                   step="0.01"
                                   class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
                        </div>

                        <div>
                            <label class="block text-sm font-medium text-gray-700">Soil Type</label>
                            <select name="soil_type" 
                                    class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
                                <option value="Black Soil" <%= land.soil_type === 'Black Soil' ? 'selected' : '' %>>Black Soil</option>
                                <option value="Red Soil" <%= land.soil_type === 'Red Soil' ? 'selected' : '' %>>Red Soil</option>
                                <option value="Alluvial Soil" <%= land.soil_type === 'Alluvial Soil' ? 'selected' : '' %>>Alluvial Soil</option>
                                <option value="Sandy Soil" <%= land.soil_type === 'Sandy Soil' ? 'selected' : '' %>>Sandy Soil</option>
                            </select>
                        </div>

                        <div class="flex justify-end space-x-4">
                            <a href="/admin/farmer-info" 
                               class="px-4 py-2 text-gray-700 bg-gray-100 rounded-md hover:bg-gray-200">
                                Cancel
                            </a>
                            <button type="submit"
                                    class="px-4 py-2 bg-green-600 text-white rounded-md hover:bg-green-700">
                                Save Changes
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
</html> 