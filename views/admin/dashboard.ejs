<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Greens Cart</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="min-h-screen flex">
        <!-- Sidebar -->
        <div class="bg-green-800 text-white w-64 py-6 flex flex-col">
            <div class="px-6 mb-8">
                <h1 class="text-2xl font-bold">Admin Dashboard</h1>
            </div>
            <nav class="flex-1">
                <a href="/admin/dashboard" class="block px-6 py-2 bg-green-700">Dashboard</a>
                <a href="/admin/users" class="block px-6 py-2 hover:bg-green-700">Manage Users</a>
                <a href="/admin/products" class="block px-6 py-2 hover:bg-green-700">Manage Products</a>
                <a href="/admin/orders" class="block px-6 py-2 hover:bg-green-700">Orders</a>
                <a href="/auth/logout" class="block px-6 py-2 hover:bg-green-700">Logout</a>
            </nav>
        </div>

        <!-- Main Content -->
        <div class="flex-1 p-8">
            <div class="mb-8">
                <h2 class="text-3xl font-bold text-gray-800">Welcome, <%= user.name %></h2>
            </div>

            <!-- Search Box -->
            <div class="mb-8">
                <form action="/admin/search-land" method="GET" class="flex gap-4">
                    <div class="flex-1">
                        <input 
                            type="text" 
                            name="landNumber" 
                            placeholder="Enter Land Number (e.g., LN001)"
                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent"
                            required
                        >
                    </div>
                    <button 
                        type="submit"
                        class="px-6 py-2 bg-green-600 text-white rounded-lg hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-green-500 focus:ring-offset-2"
                    >
                        Search
                    </button>
                </form>
            </div>

            <!-- Search Results -->
            <% if (locals.searchResult) { %>
                <div class="mb-8">
                    <h3 class="text-xl font-bold text-gray-800 mb-4">Search Result</h3>
                    <div class="bg-white rounded-lg shadow overflow-hidden">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Land Number
                                    </th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Farmer Name
                                    </th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Area of Land
                                    </th>
                                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Type of Soil
                                    </th>
                                </tr>
                            </thead>
                            <tbody class="bg-white divide-y divide-gray-200">
                                <tr class="bg-green-50">
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <%= searchResult.land_number %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <%= searchResult.farmer_name %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <%= searchResult.area %>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <%= searchResult.soil_type %>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            <% } else if (locals.error) { %>
                <div class="mb-8 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded">
                    <%= error %>
                </div>
            <% } %>

            <!-- Debug info - remove in production -->
            <div class="mb-4 p-4 bg-gray-100 rounded">
                <p>Debug Info:</p>
                <pre class="text-sm"><%= JSON.stringify({ landData: landData }, null, 2) %></pre>
            </div>

            <!-- All Land Data Table -->
            <div class="mt-8">
                <h3 class="text-xl font-bold text-gray-800 mb-4">All Farmer Lands (<%= landData ? landData.length : 0 %> records)</h3>
                <div class="bg-white rounded-lg shadow overflow-hidden">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                            <tr>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                    Land Number
                                </th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                    Farmer Name
                                </th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                    Area of Land
                                </th>
                                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                    Type of Soil
                                </th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            <% if (landData && landData.length > 0) { %>
                                <% landData.forEach(land => { %>
                                    <tr class="hover:bg-gray-50">
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <%= land.land_number %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <%= land.farmer_name %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <%= land.acres_of_land %> acres
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <%= land.soil_type %>
                                        </td>
                                    </tr>
                                <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="4" class="px-6 py-4 text-center text-gray-500">
                                        No land data available
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Dashboard Stats -->
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mt-8">
                <div class="bg-white rounded-lg shadow p-6">
                    <h3 class="text-gray-500 text-sm font-medium">Total Users</h3>
                    <p class="text-3xl font-bold text-gray-800">0</p>
                </div>
                <div class="bg-white rounded-lg shadow p-6">
                    <h3 class="text-gray-500 text-sm font-medium">Total Products</h3>
                    <p class="text-3xl font-bold text-gray-800">0</p>
                </div>
                <div class="bg-white rounded-lg shadow p-6">
                    <h3 class="text-gray-500 text-sm font-medium">Total Orders</h3>
                    <p class="text-3xl font-bold text-gray-800">0</p>
                </div>
                <div class="bg-white rounded-lg shadow p-6">
                    <h3 class="text-gray-500 text-sm font-medium">Revenue</h3>
                    <p class="text-3xl font-bold text-gray-800">$0</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 